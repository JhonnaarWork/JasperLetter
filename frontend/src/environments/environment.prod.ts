export const environment = {
  production: true,
  // Ajustar al desplegar. Vacío = rutas relativas al propio origen del frontend
  // (típico detrás de un reverse proxy que también sirve el backend).
  apiBaseUrl: ''
};
